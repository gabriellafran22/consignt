import 'package:flutter/material.dart';

class ProvinceWidget extends StatelessWidget {
  const ProvinceWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
